---
$schema: urn:oasis:names:tc:dita:xsd:topic.xsd
id: codeblock
---

# Codeblock

Code example on `for` loop:

```
for i in items:
    println(i)

```

Fenced block:

```
items.foreach(println)

```

Syntax highlighting:

```js
function add(a, b) {
  return a + b;
}
```

